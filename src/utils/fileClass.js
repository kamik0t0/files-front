export class File {
    constructor(filePath, file, folderPath, folder, content) {
        this.filePath = filePath;
        this.file = file;
        this.folderPath = folderPath;
        this.folder = folder;
        this.content = content;
    }
}
